<form method="post" action="{{ path('app_admin_client_delete', {'id': client.id}) }}" onsubmit="return confirm('Are you sure you want to Supprimer this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.id) }}">
    <button class="btn">Supprimer</button>
</form>
